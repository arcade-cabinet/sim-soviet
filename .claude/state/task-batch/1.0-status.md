---
title: SimSoviet 1.0 status
updated: 2026-04-24T03:05:00Z
status: current
domain: release
---

# SimSoviet 1.0 — Active Status Snapshot

Last update: 2026-04-24 03:05 UTC. Written to survive session compaction.

## Current shape

**P0 batch**: 7/7 complete and merged.
- P0-1 scene void fixed, P0-2 HQ button + QuotaHUD + DirectiveHUD restored,
  P0-3 fonts resolved, P0-4 debug tooltip gated, P0-5 tutorial rewired,
  P0-6 SQLite SW/COOP-COEP, P0-7 text-node errors eliminated.

**P1 batch**: 20 of ~24 tasks complete.
- Era content backfill (A-1..5) all merged: collectivization, reconstruction,
  thaw all have event coverage; victoryCondition fields removed; GPW freeze documented.
- Player flow (B-2, B-3) merged: HQ tab rescaling + ui-ux doc alignment.
- Scope cleanup (C-1..3) merged: 28 GLBs deleted, dead refs, PNG cleanup + gitignore.
- Launch readiness (D-2, D-3) docs for signed Android + iOS release paths.
- Deprecations + tests (E-1, E-2) merged.
- Verification-report gaps (F-1..6) all merged: IBM Plex Mono, directive/tutorial
  reconciliation, intro copy, classified archive entries, GPW freeze indicator,
  dev-mode SQLite warning.
- Infrastructure (#74) merged: scoped .claude/.gitignore + task-batch hooks.

**In flight**:
- **PR #76** — P1B-1 toast coalescing. The ramp slowdown in the first attempt
  broke the core test job (V8 OOM at 25min CI timeout). Reverted to original
  1-2/tick ramp but kept the coalesced onArrival callback and toast queue
  cap 3 -> 5, 3s -> 4s. Monitor bnianewcu auto-merges when core passes.

**Remaining**:
- **P1D-1** — store screenshots + metadata. Blocked: browser MCPs disconnected
  this session; P1D-1 agent hit rate limit (resets 11:30pm CT 2026-04-23).
  Next session: dispatch with Chrome DevTools MCP or Playwright restored.
- **P1D-4** — release dry-run (push v1.0.0-rc.1 tag, observe release chain).
  Depends on #76 merging first.
- Stray docs/screenshots/store/tmp-intro-modal.png left on disk by P1D-1
  pre-crash. Delete before next P1D-1 run.

## Game state summary

Tech health per last full run:
- typecheck clean, lint clean, 5,237 tests (226 suites pass + 1 skipped).
- `pnpm run build` succeeds. `pnpm run smoke:web` passes.
- Dev-mode save is silent-broken on purpose (prod works via SW); P1F-6 added a
  one-time warning.

Game journey per last verification pass:
- Landing -> Oswald + IBM Plex Mono fonts load.
- New Game -> HQ button visible, QuotaHUD + DirectiveHUD visible, Pravda ticks.
- Tutorial toasts appear in the first month. First directive aligns with first
  tutorial milestone (both point to farm) per P1F-2.
- Era transitions fire with classified-archive modals for all 7 eras per P1F-4.
- GPW (1941) shows "BUILDING PROGRAMME SUSPENDED" badge per P1F-5.
- 1991 USSRDissolutionModal fires with CONTINUE/END buttons.
- Toast flood from arrivals: coalesced to one toast per tick per P1B-1.
- Save/load works on GitHub Pages production.

Out-of-scope policy (per CLAUDE.md 2026-04-23 update): "Nothing is out of
scope. Never defer, never declare out of scope. Work additively."

## Merge ruleset on main

- required_linear_history, required_review_thread_resolution, squash-only,
  0 approvals required. Use `gh pr merge N --squash --delete-branch` and
  resolve all threads via graphql mutation before merge.

## Hooks + infra

- .claude/hooks/task-batch-flush.sh — callable + wired to PreCompact via
  settings.json. Writes .claude/state/task-batch/progress.log.
- .claude/state/task-batch/merge-log.md — authoritative P0+P1 merge record.
