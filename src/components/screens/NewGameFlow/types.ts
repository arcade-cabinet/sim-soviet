import { type GameConfig, type GameDifficulty, type GameConsequence, type MapSize } from '@/components/GameWorld';

// Re-export specific types if needed by other components, or import directly from GameWorld/SeedSystem
export type { GameDifficulty, MapSize, GameConsequence };
